const express = require('express');
const router = express.Router();

// Placeholder for multi-repo comparison metrics routing layer
router.get('/', (req, res) => res.json({ status: 'active', route: 'compare' }));

module.exports = router;
